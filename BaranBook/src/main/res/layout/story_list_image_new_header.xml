<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/storyListHeaderImage"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:scaleType="centerCrop" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="12dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/storyListHeaderTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="right"
                android:textColor="@color/FirstHeaderColor"
                android:textSize="20sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/storyListHeaderSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:gravity="right"
                android:textColor="@color/FirstHeaderColor"
                android:textSize="13sp" />
        </LinearLayout>
    </LinearLayout>

    <EditText
        android:id="@+id/storyListSearch"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="14dp"
        android:background="@drawable/story_list_new_search_bg"
        android:gravity="right|center_vertical"
        android:hint="@string/story_list_new_search_hint"
        android:paddingLeft="14dp"
        android:paddingRight="14dp"
        android:singleLine="true" />

</LinearLayout>
